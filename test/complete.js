'use strict';

var test = require('tape');
var fs = require('fs');
var parser = require('../lib/parser');


/**
 * Complete
 */

test('complete', function(assert){
  assert.plan(1);
  assert.pass();
});